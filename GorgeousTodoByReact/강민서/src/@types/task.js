/**
 * @typedef Task
 * @prop {number} id Task id
 * @prop {string} name Task name
 * @prop {boolean} completed Completion state of task
 */

/**
 * @typedef Category
 * @prop {number} id Task id
 * @prop {string} name Category name
 * @prop {string} color Category color
 * @prop {Task[]} tasks Tasks in this category
 */
